services:
  zavidovo:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
      - "3001:3001"
    environment:
      - NEXT_PUBLIC_WS_URL=ws://localhost:3001
      - PORT=3000
      - WS_PORT=3001

